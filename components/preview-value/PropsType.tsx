import React from 'React';
import { observer } from 'mobx-react';
import {TextStyle} from 'react-native';
export interface PreviewValuePropsType {
    style?: TextStyle,
    children?: React.ReactNode
}
